{% extends 'partials/base.html.twig' %}

{% block content %}
<section class="single-page-title">
    <div class="container text-center">
        <h2 class="hidden-print">{{ page.title }}</h2>
        <b class="visible-print-block">{{ page.title }}</b>
    </div>
</section>

<section class="ptb-100">
    <div class="container">
        <div class="row">
            {{ content }}
        </div>
    </div>
</section>
{% endblock %}
